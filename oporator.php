<?php
    // arithmetic  Operators examples are +,-,/,%,*,**
    $a = 3;
    $b = 5;
    $c = $a ** $b;
    echo "x".$c;
    echo "<hr/>";

    // assignment operators examples are =, +=, -=, /=, %==, *==
    $d =  6;
    $d += 4;
    echo $d;



    // comparison operators examples are <, >, <=, >=, ==(same value but can be difernt data type),
    //  ===(same datatype&value), !==(not identical), != 
    

    // logical Operator and, or &&  (and), ||(or)

    $e = "200";
    $f = "500";
    var_dump($e != $f and $e === $f);
     

?>